# Supabase client configuration for Vite (frontend)
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY

# Notes:
# - Use the anon key on the frontend only; keep service role key server-side
# - Create a .env.local file with your real values and DO NOT commit it
